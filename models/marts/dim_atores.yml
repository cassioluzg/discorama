version: 2

models:
  - name: dim_atores
    description: Dimensão atores da Discorama
    columns:
      - name: ator_filme_id
        description: Chave primária da tabela.
        tests:
          - unique
          - not_null